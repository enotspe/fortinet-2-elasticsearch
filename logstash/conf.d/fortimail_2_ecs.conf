input {
    pipeline {
        address => "fortimail_2_ecs"
    }
}

filter {

    mutate {
        rename => {
            "[tz]"=> "[event][timezone]"
        }
    }
}

output {
    pipeline {
        send_to => "common_ecs"
    }
}
